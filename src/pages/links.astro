---
export const prerender = true

import DefaultPageLayout from '$/layouts/default.astro'
import LinkPreviewList from '$/components/LinkPreviewList.astro'
import { SITE } from '../config'
import { DEFAULT_MEDIA_URL } from '../utils/media'
import { toFeedToJsonUrl, toMediaFormatFromFeed2JsonUrl } from '../utils/media-yt-feed'

/* Astro Ink's Link settings */

/* The usual stuff...*/

let title = 'リンク集';
let description = '牛久観光で役立つwebページを紹介します'

import links from '../data/links.json'

---

<DefaultPageLayout content={{ title, description }}>
    <LinkPreviewList posts={links} />
</DefaultPageLayout>


